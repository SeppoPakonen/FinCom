# Phase: Convert to Markdown

The goal of this phase is to convert all raw text files into structured Markdown files and store them in the `processed/` directory.

## Tasks
- Mapping raw text content to Markdown structure.
- Identifying headers, lists, and key information.
- Ensuring consistent formatting across all processed documents.
